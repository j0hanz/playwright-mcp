# MCP Playwright Server Configuration
# Only override these if you have specific requirements.
# All values have sensible defaults - this file is optional.

# Logging: debug | info | warn | error
# LOG_LEVEL=info

# Browser: chromium | firefox | webkit
# DEFAULT_BROWSER=chromium

# Set to false for visual debugging (shows browser window)
# HEADLESS=true

# Maximum concurrent browser sessions (default: 5)
# Increase for high-throughput scenarios, decrease for resource-constrained environments
# MAX_SESSIONS=5
